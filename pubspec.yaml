name: flutter_ecommerce_app
description: A new Flutter e-commerce application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/images/